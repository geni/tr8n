<div style="<%=tr8n_style_attribute_tag('float', 'right')%>; font-size:12px; padding-top:5px;">
  <%
    links = []
    links << link_to(tra("dashboard"), translator_path)
    links << link_to(tra("assignments"), translator_assignments_path)
    links << link_to(tra("notifications"), translator_notifications_path)
    links << link_to(tra("following"), translator_following_path)
    links << link_to(tra("preferences"), translator_settings_path)
  %>
  <%= links.join(' | ').html_safe %>
</div>
