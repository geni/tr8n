<%
  links = []
  current_tab ||= ""
  [["Topics", admin_forum_path],
   ["Messages", admin_forum_messages_path],
   ["Abuse Reports", admin_forum_reports_path]].each do |link|
      if current_page?(link.last)
        current_tab = link.first if current_tab.blank?
        links << link.first
      else
        links << link_to(link.first, link.last)
      end
	 end
%>

<div class="section_title">
	<span style="font-size:12px; float:right; padding-top:5px;">
	  <%= links.join(" | ").html_safe %>
	</span>
  <%= current_tab %>
</div>

