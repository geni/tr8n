<%= will_filter_table_tag(@results, :columns => [
  :id, :type, :description,
  [:translator_id, lambda{|rule|
    if rule.translator
      link_to(rule.translator.name, admin_translator_view_path(translator_id: rule.translator.id))
    else
      "System Generated"
    end
  }], :created_at
 ]) %>